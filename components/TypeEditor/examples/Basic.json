{
  "basic-enum": {
    "label": "basic enum",
    "schema": {
      "type": "string",
      "enum": ["apple", "bananas", "rabbit"]
    }
  },
  "basic-fm-boolean": {
    "label": "Basic FM Boolean",
    "schema": {
      "type": "integer",
      "enum": [0, 1]
    }
  },
  "ignore": {
    "label": "Ignore this field",
    "schema": {
      "ignore": "true"
    }
  }
}
